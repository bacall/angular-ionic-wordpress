import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'angular-ionic-wordpress',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
